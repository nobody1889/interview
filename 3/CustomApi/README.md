# 🚀 Telegram Bot

A simple asynchronous Telegram bot built with **Python** and **httpx**.  
The bot supports **commands** (`/start`, `/help`) and **echoing every {photo/stikers/emoji/text} messages**.  